#include <stdio.h>

/**
 * main - entry point
 *
 * Discription:
 *
 * Return: 0 (success)
*/

int main(void)
{
	for (char = 'a', char <= 'z', char++)
{
	putchar(char);
}
	putchar('\n');
	return (0);
}
